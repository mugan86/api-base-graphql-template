import { IResolvers } from "@graphql-tools/utils";

const typeConnectionResolvers: IResolvers = {
    LogConnection: {
        //user: () => /** AQUI Tratarlo */
    },
};

export default typeConnectionResolvers;